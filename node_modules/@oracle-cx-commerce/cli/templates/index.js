/*
 ** Copyright (c) 2020 Oracle and/or its affiliates.
 */

const {createWidget} = require('./widget');
const {createFetcher} = require('./fetcher');
const {createAction} = require('./actions');
const {createEndpoint} = require('./endpoints');
const {createSelector} = require('./selectors');
const {listEndpoints} = require('./endpoints/list-endpoints');
const {createSubscriber} = require('./subscriber');

module.exports = {
  createWidget,
  createFetcher,
  createAction,
  createEndpoint,
  createSelector,
  listEndpoints,
  createSubscriber
};
