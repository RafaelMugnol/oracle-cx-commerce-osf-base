import {createEndpointFactory} from '@oracle-cx-commerce/endpoints/utils/endpoint-factory';
import {getEndpointOrigin} from '@oracle-cx-commerce/commerce-utils/selector';

const API_NAME = 'oas3LocalSwaggerEndpointExample';

export const createOas3LocalSwaggerEndpointExampleEndpointFactory = createEndpointFactory({
  apiCatalogPaths: [],
  async getApiCatalog({fetchApiCatalog, state, endpointId}) {
    return {
  "listAddresses": {
    "id": "listAddresses",
    "operationId": "listAddresses",
    "url": "/addresses",
    "method": "get",
    "description": "List Addresses. This operation is used to get the list of addresses of an organization.Optionally takes the X-CCOrganization header  to specify current Organization context of logged in user.",
    "requiredParameters": [],
    "requestType": "application/json",
    "responseType": "application/json"
  }
};
  },

  getOrigin({state}) {
    return getEndpointOrigin(state, API_NAME).origin;
  },

  getHeaders() {
    const headers = {};

    return headers;
  }
});
