{
    "$schema": "http://json-schema.org/draft-06/schema#",
    "$id": "https://cloud.oracle.com/commerce-cloud/storefront/schemas/wapi-cart/switchCart/input.json",
    "title": "Switch Cart",
    "description": "Provides the ability to switch the current order context to a given incomplete order from the list of saved orders for a logged in shopper. It also supports switching the current order context with a new empty cart order for a registered shopper when no order is provided in the payload for the switchCart call.",
    "type": "object",
    "properties": {
      "orderId": {
        "type": "string",
        "description": "orderId of the order to switch"
      }
    },
    "required": [],
    "example": {
      "orderId": "o30545"
    }
  }