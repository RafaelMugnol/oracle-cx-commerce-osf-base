{
  "$schema": "http://json-schema.org/draft-06/schema#",
  "id": "https://cloud.oracle.com/commerce-cloud/storefront/schemas/wapi-payment/getPaymentGroup/input.json",
  "title": "Get Payment Group",
  "description": "Get Payment Group. Get PaymentGroup authorization status an order. Optionally takes the x-ccasset-language header to get translated content in another language.",
  "type": "object",
  "properties": {
    "paymentGroupId": {
      "type": "string",
      "description": "The id of the payment group to be fetched."
    }
  },
  "example": {
    "paymentGroupId": "pg10121",
    "exclude": "authorizationStatus,debitStatus"
  },
  "required": [
    "paymentGroupId"
  ]
}
