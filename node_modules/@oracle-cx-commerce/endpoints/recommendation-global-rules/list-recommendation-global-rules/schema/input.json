{
    "$schema": "http://json-schema.org/draft-06/schema#",
    "id": "https://cloud.oracle.com/commerce-cloud/storefront/schemas/wapi-recommendation-global-rules/listRecommendationGlobalRules/input.json",
    "title": "List Recommendation Global Rules",
    "description": "Lists Recommendations Global Rules available for the given query params.",
    "type": "object",
    "properties": {
      "fields": {
        "description": "This controls which fields are returned in the response body.",
        "type": "string"
      },
      "limit": {
        "description": "This is the field to specify the number of records to be fetched per REST call.",
        "type": "integer"
      },
      "offset": {
        "description": "This field specifies the offset/starting index from which data to be fetched.",
        "type": "integer"
      },
      "q": {
        "description": "This field specifies the query used to limit RecommendationGlobalRules",
        "type": "string"
      },
      "sort": {
        "description": "This field specifies the sort order of the list to be fetched.",
        "type": "string"
      },
      "totalResults": {
        "description": "Indicates if the total results should be included in the response.",
        "type": "boolean"
      }
    },
    "required": [],
    "example": {
      "q": "displayName CO 'brand'",
      "limit": 10,
      "offset": 2
    }
  }
