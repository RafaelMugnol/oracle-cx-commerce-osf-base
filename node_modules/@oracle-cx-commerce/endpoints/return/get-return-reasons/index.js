/*
 ** Copyright (c) 2020 Oracle and/or its affiliates.
 */

import {createEndpoint, getBodyAsJson} from '@oracle-cx-commerce/endpoints/factory';
import {populateError} from '@oracle-cx-commerce/endpoints/utils';
import {populateReturnReasons} from '@oracle-cx-commerce/endpoints/utils/transform';

export const processInput = () => {
  return {};
};

export const processOutput = async response => {
  const json = await getBodyAsJson(response);
  if (response.ok) {
    return populateReturnReasons(json);
  }

  return populateError(response, json);
};

export default createEndpoint('getReturnReasons', {
  processInput,
  processOutput
});
