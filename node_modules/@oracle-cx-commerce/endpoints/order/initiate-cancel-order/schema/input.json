{
    "$schema": "http://json-schema.org/draft-06/schema#",
    "id": "https://cloud.oracle.com/commerce-cloud/storefront/schemas/wapi-order/initiateCancelOrder/input.json",
    "title": "Initiate Cancel Order",
    "description": "Initiates cancellation of in-flight order with the specified ID. Requires the order to be within the given extended remorse period and for the currently logged-in user to be the one who created the order.",
    "type": "object",
    "properties": {
      "orderId":{
        "description": "The ID of the order to cancel.",
        "type": "string"
      },
      "cancelReason": {
        "description": "The cancel reason of the order",
        "type": "string"
      }
    },
    "required": [
      "orderId", "cancelReason"
    ],
    "example": {
      "orderId": "o10006",
      "cancelReason": "betterCompetitorPrice"
    }
  }
  