/*
 ** Copyright (c) 2020 Oracle and/or its affiliates.
 */

import {createEndpoint, getBodyAsJson} from '@oracle-cx-commerce/endpoints/factory';

import {populateError} from '@oracle-cx-commerce/endpoints/utils';
import {transformOrdersForProfile} from '@oracle-cx-commerce/endpoints/utils/transform';

const processInput = ({query, ...rest}) => {
  return {
    query: {
      q: query,
      ...rest
    }
  };
};

const processOutput = async response => {
  const json = await getBodyAsJson(response);
  if (!response.ok) {
    return populateError(response, json);
  }
  const itemsObject = transformOrdersForProfile(json.items);

  return {
    orderRepository: {
      orders: {
        ...itemsObject
      }
    },
    json
  };
};
export default createEndpoint('getAllOrdersForProfile', {processInput, processOutput});
