{
  "$schema": "http://json-schema.org/draft-06/schema#",
  "id": "https://cloud.oracle.com/commerce-cloud/storefront/schemas/wapi-profile/listProfileAddresses/input.json",
  "title": "List Profile Addresses",
  "description": "List Profile Addresses associated with the current profile.",
  "type": "object",
  "properties": {
    "offset": {
      "description": "Starting Index",
      "type": "integer"
    },
    "limit": {
      "description": "Specifies the number of addresses to be fetched starting from offset. In case this is not present, complete list of the addresses are retrieved starting from offset record.",
      "type": "integer"
    },
    "queryFormat": {
      "description": "queryFormat to search on the address properties. Possible values SCIM or null.",
      "type": "string"
    },
    "query": {
      "description": "SCIM query to search on the address properties.",
      "type": "string"
    },
    "addressType": {
      "description": "Address Type",
      "type": "string"
    },
    "sort": {
      "description": "This field determines the sort order of the list to be fetched. eg: id:asc",
      "type": "string"
    },
    "totalResults": {
      "description": "Tells whether to include total and totalResults in the SCIM query.By default the value is true",
      "type": "boolean"
    }
  },
  "example": {
    "limit": 2,
    "query": "city eq \"Dewitt\""
  },
  "required": []
}
