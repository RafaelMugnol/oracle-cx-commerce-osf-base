{
  "$schema": "http://json-schema.org/draft-06/schema#",
  "id": "https://cloud.oracle.com/commerce-cloud/storefront/schemas/wapi-share-setting/updateEmailConfigs/input.json",
  "title": "Update Email Configs",
  "description": "Update Email Configs for a given Share Setting.",
  "type": "object",
  "properties": {
    "shareSettingId": {
      "type": "string",
      "description": "Share setting to associate the email config with"
    },
    "emailConfigs": {
      "description": "Email configurations to be updated.",
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "description": "Id of the email config",
            "type": "string"
          },
          "email": {
            "description": "Email ID of the recipient.",
            "type": "string"
          },
          "editEnabled": {
            "description": "Whether the recipient has edit access for the resource being shared.",
            "type": "boolean"
          }
        }
      }
    }
  },
  "required": ["shareSettingId", "emailConfigs"],
  "example": {
    "shareSettingId": "1",
    "emailConfigs": [
      {
        "id": "E1",
        "email": "newman@example.com",
        "editEnabled": true
      }
    ]
  }
}
