/*
 ** Copyright (c) 2020 Oracle and/or its affiliates.
 */

import {createEndpoint, getBodyAsJson} from '@oracle-cx-commerce/endpoints/factory';

import {populateError} from '@oracle-cx-commerce/endpoints/utils';

export const processInput = payload => {
  console.assert(payload.purchaseListId !== undefined, 'deletePurchaseList requires a purchaseListId.');

  return {
    params: [payload.purchaseListId],
    query: {deleteShareSettings: true}
  };
};

export const processOutput = async response => {
  const json = await getBodyAsJson(response);

  return response.ok ? {} : populateError(response, json);
};

export default createEndpoint('deletePurchaseList', {processInput, processOutput});
