{
	"$schema": "http://json-schema.org/draft-06/schema#",
	"id": "https://cloud.oracle.com/commerce-cloud/storefront/schemas/wapi-scheduled-order/listScheduledOrders/input.json",
	"title": "List Scheduled Orders",
	"description": "Retrieves the scheduled orders associated with the logged in user.",
	"type": "object",
	"properties": {
		"query": {
			"description": "This field specifies the query used to filter the scheduled orders returned. Defaults to RQL. To use SCIM use the queryFormat parameter.",
			"type": "string"
		},
		"offset": {
			"description": "Starting index.",
			"type": "integer"
		},
		"limit": {
			"description": "Specifies the number of scheduled orders to be retrieved starting from offset. In case this is not present, the complete list of scheduled orders for the profile is returned.",
			"type": "integer"
		}
	},
	"example": {
		"query": "state eq \"NO_PENDING_ACTION\"",
		"offset": 0,
		"limit": 2
	}
}
