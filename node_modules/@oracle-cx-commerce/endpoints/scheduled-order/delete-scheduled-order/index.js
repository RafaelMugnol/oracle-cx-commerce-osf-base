/*
 ** Copyright (c) 2020 Oracle and/or its affiliates.
 */

import {createEndpoint, getBodyAsJson} from '@oracle-cx-commerce/endpoints/factory';
import {populateError} from '@oracle-cx-commerce/endpoints/utils';

const processInput = payload => {
  console.assert(payload.scheduledOrderId !== undefined, 'deleteScheduledOrder requires an scheduledOrderId.');
  const {scheduledOrderId} = payload;

  return {
    params: [scheduledOrderId]
  };
};

const processOutput = async response => {
  const json = await getBodyAsJson(response);

  return response.ok ? {} : populateError(response, json);
};
export default createEndpoint('deleteScheduledOrder', {processInput, processOutput});
