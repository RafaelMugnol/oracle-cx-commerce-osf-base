{
  "$schema": "http://json-schema.org/draft-06/schema#",
  "id": "https://cloud.oracle.com/commerce-cloud/storefront/schemas/store-profile/listSiteProperties/input.json",
  "title": "List Site Properties",
  "description": "Gets the list of all the site based properties for the current profile.",
  "type": "object",
  "properties": {
    "offset": {
      "type": "integer",
      "description": "StartingIndex the index of the first record to load."
    },
    "limit": {
      "type": "integer",
      "description": "The maximum number of items to retrieve."
    },
    "sort": {
      "type": "string",
      "description": "Sort Option id: Sort order."
    }
  },
  "example": {
    "offset": 0,
    "limit": 12
  }
}