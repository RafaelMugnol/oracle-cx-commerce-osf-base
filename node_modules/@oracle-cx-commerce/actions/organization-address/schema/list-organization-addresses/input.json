{
  "$schema": "http://json-schema.org/draft-06/schema#",
  "id": "https://cloud.oracle.com/commerce-cloud/storefront/schemas/store-address/listOrganizationAddresses/input.json",
  "title": "List Addresses",
  "description": "This operation is used to get the list of addresses of the current organization.",
  "type": "object",
  "properties": {
    "searchCriteria": {
      "description": "SCIM query to search on the address properties.",
      "type": "string"
    },
    "include": {
      "description": "Query string to get all/inherited secondaryAddresses.",
      "type": "string"
    },
     "offset": {
      "type": "integer",
      "description": "StartingIndex the index of the first record to load."
    },
    "limit": {
      "type": "integer",
      "description": "The maximum number of items to retrieve."
    },
    "sort": {
      "type": "string",
      "description": "Sort Option id: Sort order."
    },
    "totalResults": {
      "type": "boolean",
      "description": "Flag to include count of total products without expansion."
    },
    "queryFormat": {
      "description": "Query format to search on the address properties.",
      "type": "string"
    }
  },
  "required": [],
  "example": {
    "include": "all"
  }
}
