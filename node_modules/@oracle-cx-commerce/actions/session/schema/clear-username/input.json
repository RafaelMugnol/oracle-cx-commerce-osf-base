{
  "$schema": "http://json-schema.org/draft-06/schema#",
  "id": "https://cloud.oracle.com/commerce-cloud/storefront/schemas/store-session/clear-username/input.json",
  "title": "Remove the stored username from the session.",
  "description": "Remove the stored username from the session.  This relates to the rememberUserEmail property on the login action, if the value was set to true, the last logged in user's email address was stored (if their session expired without having logged out), this action will clear this username.",
  "type": "object",
  "properties": {
  },
  "required": [ ],
  "example": {}
}
