{
	"$schema": "http://json-schema.org/draft-06/schema#",
	"id": "https://cloud.oracle.com/commerce-cloud/storefront/schemas/store-scheduled-order/getScheduledOrder/input.json",
	"title": "Get Scheduled Order",
	"description": "Get scheduled order retrieves the scheduled order for the given id.",
	"type": "object",
	"properties": {
		"scheduledOrderId": {
			"description": "The ID of the scheduled order.",
			"type": "string"
		},
		"orderDetails": {
			"description": "Details of the templete order will be reurned if it is true.",
			"type": "boolean"
		}
	},
	"required": [
		"scheduledOrderId"
	],
	"example": {
		"scheduledOrderId": "sco10001",
		"orderDetails": true
	}
}
