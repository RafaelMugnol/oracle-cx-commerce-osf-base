{
  "$schema": "http://json-schema.org/draft-06/schema#",
  "id": "https://cloud.oracle.com/commerce-cloud/storefront/schemas/store-order-approval/rejectOrder/input.json",
  "title": "Reject Order",
  "description": "Reject Order. Approver rejects an order, those exceeds the account's purchase limit",
  "type": "object",
  "properties": {
    "orderId":{
      "description": "The order id of the order which is rejected.",
      "type": "string"
    },
    "approverMessage": {
      "description": "the message given by approver when he is rejecting order.",
      "type": "string"
    }
  },
  "required": [
    "orderId"
  ],
  "example": {
    "orderId": "o10007",
    "approverMessage": "Rejecting order"
  }
}
