{
  "$schema": "http://json-schema.org/draft-06/schema#",
  "id": "https://cloud.oracle.com/commerce-cloud/storefront/schemas/store-order-approval/checkOrderRequiresApproval/input.json",
  "title": "Check Order Requires Approval",
  "description": "Check Order Requires Approval. Order's those exceeds the account's purchase limit requires approval",
  "type": "object",
  "properties": {
    "orderId":{
      "description": "The order id of the order which requires approval.",
      "type": "string"
    }
  },
  "required": [
    "orderId"
  ],
  "example": {
    "orderId": "o10007"
  }
}
