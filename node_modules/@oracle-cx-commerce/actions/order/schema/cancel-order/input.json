{
  "$schema": "http://json-schema.org/draft-06/schema#",
  "id": "https://cloud.oracle.com/commerce-cloud/storefront/schemas/store-order/cancelOrder/input.json",
  "title": "Cancel Order",
  "description": "Cancel Order. Cancels the order with the specified id. Requires the order to be within the given remorse period and for the current logged in user to be the owner of the order.",
  "type": "object",
  "properties": {
    "orderId":{
      "description": "The ID of the order to cancel.",
      "type": "string"
    },
    "reason": {
      "description": "The cancel reason of the order",
      "type": "string"
    }
  },
  "headers":{
    "X-CCOrganization":{
      "description": "Current organization context of the logged in user",
      "type": "string",
      "required": false
    }
  },
  "required": [
    "orderId", "reason"
  ],
  "example": {
    "orderId": "o10007",
    "reason": "betterCompetitorPrice"
  }
}
