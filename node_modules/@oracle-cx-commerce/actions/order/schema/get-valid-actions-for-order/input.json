{
  "$schema": "http://json-schema.org/draft-06/schema#",
  "id": "https://cloud.oracle.com/commerce-cloud/storefront/schemas/wapi-order/getValidActionsForOrder/input.json",
  "title": "get valid actions",
  "description": "Get the valid actions for a given order.",
  "type": "object",
  "properties": {
    "orderId":{
      "description": "The ID of the order for which to retrieve the list of valid actions.",
      "type": "string"
    }
  },
  "headers":{
    "X-CCOrganization":{
      "description": "The current organization context of the logged-in user.",
      "type": "string",
      "required": false
    }
  },
  "required": [
    "orderId"
  ],
  "example": {
    "orderId": "o30411"
  }
}
