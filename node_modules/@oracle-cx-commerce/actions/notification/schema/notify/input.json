{
  "$schema": "http://json-schema.org/draft-06/schema#",
  "id": "https://cloud.oracle.com/commerce-cloud/storefront/schemas/store-notification/notify/input.json",
  "title": "Create notification",
  "description": "Creates a client wide user notification.",
  "type": "object",
  "properties": {
    "level": {
      "description": "Value indicating the type of notification. Valid values are \"info\",\"warn\" or \"error\".",
      "type": "string",
      "enum": ["info", "warn", "error"]
    },
    "message": {
      "description": "The notification message to display to the user.",
      "type": "string"
    }
  },
  "required": [
    "message",
    "level"
  ],
  "example": {
    "level": "info",
    "message": "A sample user message"
  }
}
