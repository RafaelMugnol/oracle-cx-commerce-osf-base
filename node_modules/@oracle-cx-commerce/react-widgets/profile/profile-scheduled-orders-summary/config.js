/*
 ** Copyright (c) 2020 Oracle and/or its affiliates.
 */
import * as resourceBundle from '@oracle-cx-commerce/resources';
import {buildConfigResources} from '@oracle-cx-commerce/resources/utils';

const configResourceKeys = [
  'configNumberOfUpcomingOrdersToDisplayLabel',
  'configNumberOfUpcomingOrdersToDisplayHelpText'
];

const config = {
  properties: [
    {
      id: 'numberOfUpcomingOrdersToDisplay',
      type: 'stringType',
      helpTextResourceId: 'configNumberOfUpcomingOrdersToDisplayHelpText',
      labelResourceId: 'configNumberOfUpcomingOrdersToDisplayLabel',
      defaultValue: '5',
      pattern: '^[0-9]*$'
    }
  ],
  locales: buildConfigResources(resourceBundle, configResourceKeys)
};

export default config;
