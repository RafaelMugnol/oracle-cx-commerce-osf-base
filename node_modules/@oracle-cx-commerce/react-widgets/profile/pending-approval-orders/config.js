/*
 ** Copyright (c) 2020 Oracle and/or its affiliates.
 */
import * as resourceBundle from '@oracle-cx-commerce/resources';
import {buildConfigResources} from '@oracle-cx-commerce/resources/utils';

const configResourceKeys = ['configOrdersPerPageSelectorLabel', 'configOrdersPerPageSelectorHelpText'];

const config = {
  properties: [
    {
      id: 'itemsPerPageList',
      type: 'stringType',
      helpTextResourceId: 'configOrdersPerPageSelectorHelpText',
      labelResourceId: 'configOrdersPerPageSelectorLabel',
      defaultValue: '16,32,48,64',
      pattern: '^[0-9,]*$'
    }
  ],
  locales: buildConfigResources(resourceBundle, configResourceKeys)
};

export default config;
