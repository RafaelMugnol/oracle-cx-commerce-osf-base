/*
 ** Copyright (c) 2020 Oracle and/or its affiliates.
 */

import * as resourceBundle from '@oracle-cx-commerce/resources';

import {buildConfigResources} from '@oracle-cx-commerce/resources/utils';
import {mergeDefaultConfig} from '@oracle-cx-commerce/react-widgets/config';

export const contactRegistrationRequestConfigResourceKeys = ['configNumberOfMostRecentRequestsLabel'];
export const contactRegistrationRequestConfigProperties = [
  {
    id: 'numberOfRequestsToLoad',
    type: 'stringType',
    labelResourceId: 'configNumberOfMostRecentRequestsLabel',
    defaultValue: '10',
    pattern: '^[0-9]*$'
  }
];

const contactRegistrationRequestConfig = mergeDefaultConfig({
  properties: contactRegistrationRequestConfigProperties,
  locales: buildConfigResources(resourceBundle, contactRegistrationRequestConfigResourceKeys)
});

export default contactRegistrationRequestConfig;
