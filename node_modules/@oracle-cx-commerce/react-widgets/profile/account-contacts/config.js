/*
 ** Copyright (c) 2020 Oracle and/or its affiliates.
 */

import * as resourceBundle from '@oracle-cx-commerce/resources';

import {buildConfigResources} from '@oracle-cx-commerce/resources/utils';
import {mergeDefaultConfig} from '@oracle-cx-commerce/react-widgets/config';

const configResourceKeys = ['labelContactsPerPageSelector', 'helpTextContactsPerPageSelector'];
const config = mergeDefaultConfig({
  properties: [
    {
      id: 'itemsPerPageList',
      type: 'stringType',
      helpTextResourceId: 'helpTextContactsPerPageSelector',
      labelResourceId: 'labelContactsPerPageSelector',
      defaultValue: '16,32,48,64'
    }
  ],
  locales: buildConfigResources(resourceBundle, configResourceKeys)
});

export default config;
