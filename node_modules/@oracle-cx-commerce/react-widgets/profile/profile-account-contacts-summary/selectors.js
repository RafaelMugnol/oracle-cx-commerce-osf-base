/*
 ** Copyright (c) 2020 Oracle and/or its affiliates.
 */

import {getRolesForProfile} from '@oracle-cx-commerce/commerce-utils/selector';

export const getComponentData = state => {
  const roles = getRolesForProfile(state);

  return {
    roles
  };
};
