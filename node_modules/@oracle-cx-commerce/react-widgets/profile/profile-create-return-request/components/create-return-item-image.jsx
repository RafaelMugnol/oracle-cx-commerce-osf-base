/*
 ** Copyright (c) 2020 Oracle and/or its affiliates.
 */

import React from 'react';
import css from '@oracle-cx-commerce/react-widgets/profile/profile-create-return-request/styles.css';
import Img from '@oracle-cx-commerce/react-components/img';
import Styled from '@oracle-cx-commerce/react-components/styled';

export const CreateReturnItemImage = props => {
  const {orderData} = props;
  const {commerceItemId} = props;

  return (
    <Styled id="CreateReturnItemImage" css={css}>
      <div className="CreateReturnItemImage">
        <Img
          src={orderData.commerceItems[commerceItemId].primaryThumbImageURL}
          alt={orderData.commerceItems[commerceItemId].displayName}
          title={orderData.commerceItems[commerceItemId].displayName}
        />
      </div>
    </Styled>
  );
};
