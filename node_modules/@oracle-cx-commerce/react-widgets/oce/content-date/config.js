/*
 ** Copyright (c) 2020 Oracle and/or its affiliates.
 */
import {buildConfigResources} from '@oracle-cx-commerce/resources/utils';
import * as resourceBundle from '@oracle-cx-commerce/resources';
import {mergeDefaultConfig} from '@oracle-cx-commerce/react-widgets/config';

const contentDateConfigResourceKeys = ['configContentTextPickerLabel', 'configContentTextHelpText'];
const contentDateConfigProperties = [
  {
    id: 'settings',
    type: 'contentPickerType',
    helpTextResourceId: 'configContentDateHelpText',
    labelResourceId: 'configContentDatePickerLabel',
    required: true
  }
];

const config = mergeDefaultConfig({
  properties: [...contentDateConfigProperties],
  locales: buildConfigResources(resourceBundle, contentDateConfigResourceKeys)
});
export default config;
