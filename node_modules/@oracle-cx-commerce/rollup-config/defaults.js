/*
 ** Copyright (c) 2020 Oracle and/or its affiliates.
 */

/**
 * This is the default Rollup configuration file that "yarn build" uses
 * if an application does not supply its own rollup.config.js file in
 * its root directory (i.e. "packages/apps/app-name/rollup.config.js").
 */
const {createBuildConfigs} = require('./index');

module.exports = createBuildConfigs();
